# Azure OpenAI model used as the "judge" for AI-assisted evaluators
judge_model:
  azure_endpoint: ${AZURE_OPENAI_ENDPOINT}
  api_key: ${AZURE_OPENAI_API_KEY}
  azure_deployment: ${AZURE_OPENAI_DEPLOYMENT}  # e.g. gpt-4o
  api_version: "2024-12-01-preview"
